import 'package:flutter/material.dart';

class InvestmentsTabScreen extends StatelessWidget {
  const InvestmentsTabScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
