import 'package:flutter/material.dart';

class SavingsTabScreen extends StatelessWidget {
  const SavingsTabScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
