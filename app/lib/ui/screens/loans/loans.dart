import 'package:flutter/material.dart';

class LoansTabScreen extends StatelessWidget {
  const LoansTabScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
